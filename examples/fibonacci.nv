// Sequência de Fibonacci

func fibonacci(n) {
    if n <= 1 {
        return n
    }
    return fibonacci(n - 1) + fibonacci(n - 2)
}

say "Sequência de Fibonacci:"
say "Os primeiros 10 números:"

loop 10 {
    let resultado = fibonacci(loop_index)
    say resultado
}
